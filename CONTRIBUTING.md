# 贡献指南

## 开始前

- 功能开发、数据导入和页面验证都应先在测试环境完成；不要直接修改生产数据库或生产目录。
- 使用 `.env.example` 创建本地 `.env`，并提供 `DATABASE_URL`。`.env`、数据库备份和运行日志不得提交。
- 提交前请确认没有密码、访问令牌、私钥、客户数据或测试日志进入 Git 工作区。

## 开发与验证

1. 在独立分支完成改动。
2. 后端改动至少执行 Python 语法检查；前端改动执行 `npm run build`。
3. 对涉及查询、统计、导入或导出的改动，在测试库验证结果。
4. 在变更说明中写清楚用户可见行为、数据库影响和回退方式。

## 提交与评审

- 建议提交信息采用 `feat:`、`fix:`、`docs:`、`refactor:` 或 `chore:` 前缀。
- 一个提交聚焦一个目的；数据库迁移、数据修复和普通功能改动应分开。
- 合并前由维护者确认测试结果，并在需要时更新 `CHANGELOG.md`。

# 发布流程

当前版本映射：

- `v1.0.0`：原 `main` 分支的稳定基线。
- `v1.2.0`：包含 MODS、综合判定、包装前验证与发布资料的当前版本。

后续版本按以下顺序发布：

1. 在测试环境完成回归验证，并确认生产部署和回退方案。
2. 更新 `VERSION`、前端 `package.json` 和 `CHANGELOG.md`，三者使用相同版本号。
3. 创建独立发布提交，例如 `chore: prepare v1.3.0`。
4. 创建带注释的 Git 标签：`v1.3.0`。
5. 推送提交和标签后，在 GitHub 基于该标签创建 Release；Release Notes 以 `CHANGELOG.md` 对应版本为准。
6. 发布后记录部署时间、部署目标、数据库变更和回退点。

任何包含数据库结构或数据修复的版本，都必须在 Release Notes 中明确标注升级与回退步骤。
